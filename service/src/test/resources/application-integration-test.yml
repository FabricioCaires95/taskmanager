# =================================
# = Spring
# =================================
spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:10://localhost/db_taskmanager?TC_INITSCRIPT=file:src/test/resources/init.sql
    username: admin
    password: admin
  main:
    allow-bean-definition-overriding: true
  flyway:
    locations: classpath:/db/migration/postgresql
